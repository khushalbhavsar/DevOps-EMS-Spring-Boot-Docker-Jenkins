# Docker Compose for Local Development with Monitoring
# NOTE: SonarQube is excluded by default (already running as standalone service on server)
# To include SonarQube, run: docker compose --profile with-sonarqube up -d

services:
  employee-management:
    build: .
    ports:
      - "8081:8080"  # External 8081 -> Container 8080 (Jenkins uses 8080)
    environment:
      - SPRING_PROFILES_ACTIVE=local
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:testdb
      - MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,metrics,prometheus
    volumes:
      - ./logs:/app/logs
    networks:
      - app-network

  # SonarQube for code quality analysis (OPTIONAL - use profile to enable)
  # Already running as standalone service on server at localhost:9000
  sonarqube:
    image: sonarqube:community
    ports:
      - "9000:9000"
    environment:
      - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    networks:
      - app-network
    profiles:
      - with-sonarqube

  # Prometheus for metrics collection (OPTIONAL - use profile to enable)
  # Already running as standalone service on server at localhost:9090
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - app-network
    profiles:
      - with-monitoring

  # Grafana for metrics visualization (OPTIONAL - use profile to enable)
  # Already running as standalone service on server at localhost:3000
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=grafana-piechart-panel
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana-datasource.yml:/etc/grafana/provisioning/datasources/datasource.yml
    networks:
      - app-network
    depends_on:
      - prometheus
    profiles:
      - with-monitoring

  # Optional: Add database service for production-like testing
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: employeedb
      POSTGRES_USER: employee_user
      POSTGRES_PASSWORD: employee_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    profiles:
      - with-db

volumes:
  postgres_data:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
  prometheus_data:
  grafana_data:

networks:
  app-network:
    driver: bridge